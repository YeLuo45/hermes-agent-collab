# Hermes Agent Collaboration - Python Dependencies
fastapi>=0.100.0
uvicorn[standard]>=0.23.0
pydantic>=2.0.0
websockets>=11.0
httpx>=0.25.0

# PostgreSQL backend (Direction I)
asyncpg>=0.29.0
psycopg2-binary>=2.9.9

# Metrics / Observability (Direction J)
prometheus-client>=0.19.0

# Redis Channel Adapter (Direction L)
redis>=5.0.0

# Tracing / Observability (Direction P)
opentelemetry-api>=1.20.0
opentelemetry-sdk>=1.20.0
opentelemetry-exporter-otlp>=1.20.0
opentelemetry-instrumentation-fastapi>=0.41b0
opentelemetry-instrumentation-httpx>=0.41b0

# Config Hot-Reload (Direction R)
watchdog>=3.0.0
pyyaml>=6.0
cryptography>=41.0.0

# gRPC interface (Direction AB)
grpcio>=1.60.0
grpcio-tools>=1.60.0

# Config validation (Direction AC)
jsonschema>=4.20.0
referencing>=0.30.0

# Dev Dependencies
pytest>=8.0.0
pytest-asyncio>=0.23.0
